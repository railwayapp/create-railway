{
  "title": "Railway Sandbox",
  "description": "Ephemeral compute: create, exec, destroy.",
  "env": ["RAILWAY_API_TOKEN", "RAILWAY_ENVIRONMENT_ID"],
  "nextSteps": ["{{pm}} install", "cp .env.example .env  # then fill in your credentials", "{{pm}} start"],
  "symlinks": { "CLAUDE.md": "AGENTS.md" }
}
